# ATLAS PROJECT LOG UPDATE PACK — Wix GitHub Environment Setup — 22:57, 29.07.2026

## Session / task

Configure `nanotech-solutions-norway/atlas_wix` for Wix CLI local development, validate the Atlas-AI Wix/GitHub connection and preserve the Atlas release controls.

## Issue or mistake observed

1. The Wix-generated repository README stated Node.js 14.8 or later, while current official Wix Git Integration setup guidance requires Node.js 20.11 or later.
2. The Wix-generated repository contained the required site code and connection metadata but did not include Atlas-specific branch governance, release locks, secret-safe local-environment controls or automated connection-metadata validation.
3. A package-lock generation attempt in the execution environment could not resolve the required Wix package through the available npm path; one attempt returned package-not-found and a direct registry attempt timed out.

## Root cause

1. Provider-generated repository boilerplate had not been reconciled with current official Wix documentation.
2. Wix Git Integration creates the technical site-code baseline but does not encode the Atlas canonical source hierarchy, PR-only workflow or release-governance requirements.
3. The execution environment's npm registry/network path was unavailable for deterministic local lockfile generation. No credentials or secrets were used or exposed.

## Corrective rule

- Reconcile provider-generated repository boilerplate against current official provider documentation before adopting it as a current environment requirement.
- Keep `nanotech-solutions-norway/atlas_wix` as the Wix site-code repository while `nanotech-solutions-norway/Atlas` remains the canonical governance and release-history source.
- Require feature branches and pull requests for changes to `main`; do not push feature work directly to `main`.
- Validate the Wix site identity in `wix.config.json`, required repository structure, Node requirement, package scripts and ignored local/secret files in CI.
- Report repository/site metadata validation separately from an interactive developer-workstation `wix dev` acceptance test.
- Never store Wix authentication, provider credentials, API keys or tokens in GitHub Actions or frontend code.

## Evidence / source

- Explicit user instruction and Wix Local Dev Setup screenshot, 29.07.2026.
- Current official Wix Git Integration and Wix CLI setup documentation.
- Authenticated Wix context for `Atlas-AI`, site ID `1448ccad-68f0-43ea-8f0e-c9d8c0366082`, Wix Studio and Velo enabled.
- GitHub initial commit `1c3680ab56b9cf1b737e3f0b9436b2dca5dc3040`, authored and committed by `velo-app[bot]`.
- Repository `wix.config.json` with the matching Atlas-AI site ID.
- `atlas_wix` pull request #1 and successful GitHub Actions run `30490322423`.
- Squash merge commit `692a38a70c6b24e6745ec4251972587aebbab61c`.
- Controlled Google Drive validation record under `Working Records - July 2026/02_Wix_Audits_Roadmaps_and_Readiness`.

## Action taken

- Created branch `chore/wix-local-dev-environment-20260729` in `atlas_wix`.
- Updated `package.json`, `.gitignore` and README.
- Added `.nvmrc`, `.npmrc`, `scripts/validate-wix-repository.mjs`, a GitHub Actions validation workflow and a controlled repository connection-validation report.
- Opened pull request #1, validated the pull-request workflow and squash-merged the PR to `main`.
- Created the Google Drive validation report and this controlled Atlas project log update pack.

## Validation performed

| Target | Result |
|---|---|
| `atlas_wix` repository exists and is private | `PASS` |
| Default branch is `main` | `PASS` |
| Initial repository commit was generated by the Wix/Velo app | `PASS` |
| Repository Wix site ID matches the authenticated Atlas-AI Wix site ID | `PASS` |
| Atlas-AI is Wix Studio with Velo enabled | `PASS` |
| Node requirement aligned to current Wix setup guidance | `PASS` |
| Pull-request GitHub Actions validation | `PASS` |
| Controlled squash merge to `main` | `PASS` |
| Interactive `wix dev` Local Editor session | `NOT_RUN` — requires an authorized developer workstation and browser login |
| Public/production release | `NO_GO` |

## Unresolved items

- Run the documented setup commands on an authorized developer workstation and confirm that `wix dev` opens the Atlas-AI Local Editor without authentication or synchronization errors.
- Generate and commit a deterministic package lock only from an environment that successfully resolves the Wix packages and after dependency review.
- Continue the existing Atlas implementation and acceptance gates before publication or production activation.

## Preserved controls

- `publicAllowed=false`
- `purchaseAllowed=false`
- `releaseApproved=false`
- No Wix publication or production availability change.
- No payment or checkout activation.
- No public write endpoint.
- No frontend provider token.
- No provider or Wix credentials stored.
- Release state remains `NO_GO`.

## Classification

`AUTO_APPROVED`:
- exact verified repository, Wix identity, configuration, PR, CI and merge facts;
- current Wix prerequisite reconciliation;
- non-sensitive workflow, security and quality-control improvements;
- Google Drive controlled write-back.

`PENDING_REVIEW`:
- none for the environment configuration itself;
- all production publication, purchase, payment, provider-routing and release decisions remain governed separately and unchanged.
